# Mizo Times Tables — site

Privacy policy for **جدول الضرب ميزو** (`com.hossam.timestables`), served by GitHub Pages.

- Arabic and English, switched with `?lang=ar` / `?lang=en`, defaulting to the browser's language
- The source of truth is `design/web/privacy.html` in the app repository

The app collects no data, so the policy is short and says exactly that.
